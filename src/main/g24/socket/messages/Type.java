package main.g24.socket.messages;

public enum Type {
    BACKUP,
    DELKEY,
    DELCOPY,
    GETFILE,
    FILEHERE,
    FILEEXISTS,
    REPLICATE,
    REPLICATED,
    ACK,
    REMOVED,
    STATE,
}
